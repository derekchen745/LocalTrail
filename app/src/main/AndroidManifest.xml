<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalTrail"
        tools:targetApi="31" >
        <activity
            android:name=".controller.activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.LocalTrail"
            android:label="@string/app_name" />
        <activity
            android:name=".controller.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.activities.CreateAccountActivity"
            android:exported="true"
            android:theme="@style/Theme.LocalTrail"
            android:label="@string/app_name" />
        <activity
            android:name=".view.trail.TrailDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalTrail"
            android:label="@string/app_name" />
        <activity android:name=".controller.activities.FriendsActivity" />
        <activity
            android:name=".controller.activities.FriendRequestsActivity"
            android:exported="true"
            android:theme="@style/Theme.LocalTrail"
            android:label="@string/app_name" />
        <activity android:name=".controller.activities.AddFriendsActivity" />
        <meta-data
            android:name="com.mapbox.token"
            android:value="${MAPBOX_ACCESS_TOKEN}" />
    </application>

</manifest>